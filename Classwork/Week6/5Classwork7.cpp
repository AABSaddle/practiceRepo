#include <iostream>

using namespace std;

int main()
{
    string phrase;
    cout << "Please input a phrase and ill reverse it: ";
    getline(cin, phrase);
    string placeHolder = "";
    string = revOrder = "";
    for(int i = 0; i < phrase.length(); i++)
    {
        if(placeHolder == "")
        {
            placeHolder += phrase[i];
        }
        if()
    }
    cout << first << endl;
}